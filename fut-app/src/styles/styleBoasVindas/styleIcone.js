import { StyleSheet } from 'react-native';

export const icone = StyleSheet.create({
    icone: {
        display: "flex",
        flexDirection: "row",
        gap: 8,
        alignItems: 'flex-end',
        alignSelf: 'flex-end'
    }
});
