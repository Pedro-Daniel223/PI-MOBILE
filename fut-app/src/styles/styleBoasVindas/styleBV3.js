import { StyleSheet } from 'react-native';

export const stylesBV3 = StyleSheet.create({
    conteudo:{
        height: '50%',
        width: '100%',
        justifyContent: 'space-between',
        marginBottom: 100,
        alignItems: 'center'
    }
});
